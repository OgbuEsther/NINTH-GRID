USE ninthgrid;

CREATE TABLE IF NOT EXISTS Photos (
    id INT PRIMARY KEY AUTO_INCREMENT,
    photoName VARCHAR(50),
    photoUrl VARCHAR(100),
    details VARCHAR(150)
);

INSERT INTO Photos (photoName , photoName , details) VALUES 


SELECT * FROM Photos;

SHOW TABLES IN ninthgrid;
